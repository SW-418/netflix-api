spring:
  application:
    name: netflix-api

  datasource:
    url: jdbc:h2:mem:netflix-api-test;DB_CLOSE_DELAY=-1

  flyway:
    enabled: true

  threads:
    virtual:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
